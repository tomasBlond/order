pipeline {
    agent any

    tools {
        jdk 'jdk-12'
		maven 'maven'
    }

    stages {
        stage ('Build Project') {
            steps {
                sh 'mvn clean test-compile'
            }
        }
    }
}
